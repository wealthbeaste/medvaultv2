# Railway Deployment Configuration
# Railway reads this file to know how to run your app

[build]
builder = "nixpacks"

[deploy]
startCommand = "node server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
